import { Typography } from "@mui/material";
import Layout from "Layout";

function wallet() {
    return ( 
        <Layout>
            <Typography variant="h3">This is Wallet Page</Typography>
        </Layout>
     );
}

export default wallet;